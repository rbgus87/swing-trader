# ── Core ──
httpx>=0.27
websockets>=12.0
pykrx>=1.0
pandas>=2.0
pandas-ta>=0.3.14b
numpy>=1.24
APScheduler>=3.10
loguru>=0.7
requests>=2.31
python-dotenv>=1.0
PyYAML>=6.0
holidays>=0.45
pytz>=2023.3

# NOTE: Python 3.14+에서는 pandas-ta를 --no-deps로 설치하세요:
#   pip install pandas-ta --no-deps
# pandas-ta는 numba 없이도 정상 동작합니다.

# ── Dev / Test ──
pytest>=7.4
pytest-cov>=4.0
requests-mock>=1.12
pytest-asyncio>=0.23
black>=23.0
ruff>=0.1
