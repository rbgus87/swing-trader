# ── Core ──
httpx>=0.27
websockets>=12.0
pykrx>=1.0
pandas>=2.0
numpy>=1.24
APScheduler>=3.10
loguru>=0.7
requests>=2.31
python-dotenv>=1.0
PyYAML>=6.0
ruamel.yaml>=0.18
holidays>=0.45
pytz>=2023.3
matplotlib>=3.7
pyqtgraph>=0.13
streamlit>=1.30
pydantic>=2.0

# pandas-ta: numba 의존성 문제로 별도 설치 필요
# pip install pandas-ta --no-deps
# (requirements.txt에 넣으면 Python 3.14에서 numba 빌드 에러 발생)

# ── Dev / Test ──
pytest>=7.4
pytest-cov>=4.0
requests-mock>=1.12
pytest-asyncio>=0.23
black>=23.0
ruff>=0.1
